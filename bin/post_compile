#!/usr/bin/env bash
set -eo pipefail

echo "-----> Running post-compile hook"

# Get the directory where the script is located
BUILD_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
echo "Build directory: $BUILD_DIR"

# Create necessary directories
mkdir -p $BUILD_DIR/staticfiles $BUILD_DIR/mediafiles

# Run migrations
echo "-----> Running database migrations"
python $BUILD_DIR/manage.py migrate --noinput

# Collect static files
echo "-----> Collecting static files"
python $BUILD_DIR/manage.py collectstatic --noinput

# Create superuser if needed
echo "-----> Creating superuser if not exists"
python $BUILD_DIR/manage.py shell << EOF
from django.contrib.auth.models import User
if not User.objects.filter(username='${DJANGO_SUPERUSER_USERNAME}').exists():
    User.objects.create_superuser('${DJANGO_SUPERUSER_USERNAME}', '${DJANGO_SUPERUSER_EMAIL}', '${DJANGO_SUPERUSER_PASSWORD}')
    print('Superuser created successfully')
else:
    print('Superuser already exists')
EOF

echo "-----> Post-compile hook completed"